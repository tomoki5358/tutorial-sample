apiVersion: vs.axis-dev.io/v1
kind: AssemblyLine
metadata:
  name: nginx-demo-ogawa-azure
spec:
  params:
    - name: gitRevision
      type: string
    - name: imageName
      type: string
  stages:
    - name: deploy
      spec:
        pipeline: nginx-demo-ogawa-deploy
        deployment:
          app: nginx-demo-ogawa
          name: stating-nginx
        params:
          - name: gitRevision
            value: $(inputs.gitRevision)
          - name: imageName
            value: $(inputs.imageName)
    - name: resolve-ip-address
      spec:
        pipeline: nginx-demo-ogawa-resolve-ip-address-after-deploy
        deployment:
          app: nginx-demo-ogawa
          name: stating-nginx
      runAfter:
        - deploy
  results:
    - name: ipAddress
      value: $(stages.resolve-ip-address.results.ipAddress)
